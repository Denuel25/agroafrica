// index.test.js
test('simple test', () => {
    expect(true).toBe(true);
  });
  import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>Welcome to AgroAfrica 🌱</h1>;

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);

